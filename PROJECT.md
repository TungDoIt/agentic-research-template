# Research brief

Human-owned; the agent may draft this brief from the discussion during requested setup (see [README.md](README.md#set-up-from-the-discussion)). Direct editing is also supported. Only the objective is required; omit unused optional sections. Capture human intent here and put inferred success criteria, defaults, and assumptions in `STATE.md`. The agent must not treat unfilled placeholders as research facts.

## Objective

<The question to answer or result to establish.>

## Context (optional)

<Relevant context from the discussion: private background, supplied observations or hypotheses, definitions, links to supplied files/data/resources, and important unknowns. Attribute unverified claims to the human. Add `inputs/` only if useful for supplied originals. A paragraph or two is enough; no literature review is required.>

## Constraints and stopping conditions (optional)

<Human-specified scope exclusions, confidentiality or tool restrictions, resource/time limits, desired outputs, stopping conditions, or criteria that would make the answer useful. Omit anything not specified; the agent infers practical success criteria in `STATE.md`.>
